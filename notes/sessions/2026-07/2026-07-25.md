# 2026-07-25 — GROW: Sluice gets the "depth inside the mechanic" layer (12th game)

_Newest on top._

## v0.27.0 — STANDARDS: adopt the fairyfox hub batch 0.20.2 → 1.6.1 in full

**Trigger.** Owner: "fairyfox system update in full … proceed … in as many phases as needed …
reach completion in full." A fairyfox check/adopt run — the word *fairyfox* + an update intent.

**Anchor + refresh.** Last-adopted `hub_version` was **0.20.2** (report 2026-07-20). Refreshed
the git-ignored hub mirror (`assets/references/fairyfox.io`): clean fast-forward `697bc5c →
2d614f0`, hub VERSION **0.20.2 → 1.6.1** (123 files, +6216/−263). Own working tree was clean on
`dev`, synced with origin.

**Pre-authorization.** `authorizations.yml` carries the standing `adopt-standards-by-default`
(+ `express-authorization-rollout`) grant covering all of `hub/standards/` + `hub/templates/`,
so the confirm-pause was skipped — but the full safety floor kept (copy-not-clobber, verify
before + after, reviewable PR release, this report).

**What I did (phased, per the plan in `notes/plans/2026-07-25-fairyfox-standards-adoption.md`).**

1. **Vendored** the 5 new + ~24 changed `hub/standards/*.md` into `notes/reference/` (verbatim
   mirrors; confirmed the standards docs carry no local appendices — real divergences live in
   the chrome/asset layer). New: `checklists-are-contracts`, `mandate-ledger`, `docker`,
   `readme-structure`, `ci-secrets`. Updated the `reference/README.md` index.
2. **Gotcha (worth a hub note):** copying the hub's `readme.md` clobbered the project's
   `README.md` index — **Windows filesystem is case-insensitive**, so `readme.md` == `README.md`.
   Restored the index (`git checkout`), re-vendored the standard as **`readme-structure.md`**,
   recorded the rename as a deliberate divergence in the index + manifest.
3. **Created `notes/reference/adoption-manifest.md`** — the new required per-standard record
   (implemented / copied-only / gap / N-A), honestly filled, remainders named.
4. **Concrete deliverables:** README → full ordered **20-badge** block + docs link + "Get it"
   section + mesh footer (per `badges.md` + `readme-structure.md`); CLAUDE.md gained the Docker
   **N-A** note + phase-by-default nod; added `scripts/check-standards.mjs`.
5. **Verify (before + after):** `node --test "games/**/*.test.js"` → **783/783**; `bundle exec
   jekyll build` clean; `check-links` 108 files OK; `check-standards` OK. `check-tidy`'s only
   "stranded" files were the new untracked ones (resolved on stage).

**Remainder (owned gaps, not silent drops).** Coverage/CodeFactor/SonarCloud badge slots are
present but the services are unwired → manifest `gap(owner call)`. docs-site chrome re-verify is
browser-gated → manifest `gap(next preview session)`. Both dated in the manifest.

**Release.** MINOR bump 0.26.1 → **0.27.0** (standards milestone); `dev` → `main` via PR
(branch-protected), hand-tag `v0.27.0` (release.yml reacts). See the process report
`notes/fairyfox-reports/2026-07-25-adopting-updates.md`.

## v0.26.1 — GROW: Sluice depth — flash · Spate · no-plateau fall · secret Charybdis

**Why Sluice.** GROW run (`fairyfox-games-daily`). Varied-structure rollout is complete; the
active lever is the "depth inside the mechanic" layer. Sluice was the last game still named as
missing it (prior status: *depth rollout 11 of 13, left: Sluice*), so it was the clear pick.
The newer games (Tether/Reprise/Ward/Drove/Brim/Loom) already ship depth from birth; this
clears the retrofit list.

**What Sluice already had.** Varied structure (formations Steady/Run/Alternate/Shuffle/Cascade/
Rush/The Churn, `pickFormation`/`loadFormation`, `minStage`-gated), a stage arc (Trickle →
Maelstrom), a snap-combo (route within `SNAP_FRAC` of the fall grows the ×2…×9 multiplier),
and meta (`sluice.meta`, 10 badges). Missing: the four depth items.

**The four items (all on the one *route* verb):**

- **Flash — the hidden tech.** A route inside the razor **`FLASH_FRAC` 0.12** sub-window (well
  tighter than the `SNAP_FRAC` 0.45 snap) — reading a freshly-scrambled row and committing
  almost the instant the spark appears — pays a flat **`FLASH_BONUS` 2** on top of the snap,
  blooms gold, and builds a streak. Taught nowhere. A plain snap or a miss silently breaks the
  flash chain. (A flash is always also a snap, so it grows the multiplier too.) Fits Sluice's
  own hook exactly: the depth *was* "when you commit," so an even-earlier commit is the ceiling.
- **Spate — the streak reward.** `FLASH_STREAK` 3 flashes in a row → `SPATE_TICKS` 300 (~5s)
  where **every point doubles**. The triggering flash is *not* doubled — `doubling` reads
  `g.spate > 0` before the resolution scores, and the window is armed after. `tick` counts it
  down. Gold vignette (colour-only; static under reduced motion).
- **No-plateau fall asymptote.** `fallTicksOf` rewritten: `FALL_HARD_MIN 30 + (FALL_BASE 108 −
  FALL_HARD_MIN) · exp(−cleared / FALL_TAU 130)` — approaches but never reaches the floor, so
  late sparks keep quickening forever. Replaced the old linear `FALL_BASE − cleared·FALL_DEC`
  clamped at `FALL_MIN 40`, which flat-lined at cleared ≈ 136. Opening unchanged (cleared 0 =
  108). `spawnDrop` no longer clamps the non-fast path at 40 (that would re-introduce the
  plateau); fast sparks get a separate `FAST_MIN 22` floor.
- **Secret Charybdis stage.** STAGES gains a 6th entry (`at` 180, `secret: true`). `stageProgress`
  hides a secret *next* stage from the HUD (the bar still fills toward it, nameless) until you
  cross into it. Printed on no start screen; the start tip's stage ladder + formation-name list
  were trimmed (teach-by-play).

**Bookkeeping.** 3 new badges (10 → 13: `flash-hand` = 10 flashes/run, `spate` = raise one,
`charybdis` = reach the hidden stage), `totals.flashes` added to meta (lossless legacy upgrade),
run report names flashes/spates. Shell: gold flash channel overlay + Spate banner/vignette,
summary carries `flashes`/`spates`.

**Tests.** Rewrote the fall-time test for the asymptote (monotonic descent; never below the
floor; still descending deep = no plateau). Adjusted the snap test (elapsed 0 is now a flash, so
age into the snap-but-not-flash band). Added flash/Spate tests (bonus, correct-only, streak
break, trigger-not-doubled, doubling isolation + window countdown), a secret-stage test
(name withheld until reached), and depth-badge + `totals.flashes` meta tests. Sluice 39 → **45**
test functions; full collection **783/783** green (`node --test "games/**/*.test.js"`).

**Maintenance.** No open issues. 4 Dependabot PRs open (GitHub-Actions version bumps: checkout
7.0.1, setup-ruby 1.321.0, setup-node 7.0.0, attest-build-provenance 4.1.1) — left for their own
auto-flow, noted in next-steps; not bundled into this release.

**Player-facing log.** Prepended a `grow` entry to `_data/changelog.json`; bumped
`_games/sluice.md` `updated:` to 2026-07-25.
