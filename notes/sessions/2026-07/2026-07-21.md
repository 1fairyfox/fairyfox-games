# 2026-07-21 — PLANT: Drove (16th game) · GROW: Loft joins the depth layer (8/13)

_Scheduled PLANT run (`fairyfox-games-new`), after the day's GROW run below._

## v0.25.0 — PLANT: Drove — herd/shepherd, the first indirect-control game

- **Maintain:** no open issues; PRs #46/#31 (Dependabot majors) still deliberately held —
  unchanged from the morning's GROW run.
- **Verb check:** surveyed all 15 games + plans; every existing verb acts on the world
  directly (steer, catch, thrust, flip, aim, stack, keep-aloft, balance, mirror, charge,
  sort, pour, swing, recall, guard). Drove's **herd** is indirect control — you move the
  thing the quarry flees from. Genuinely distinct, not a re-skin.
- **The mechanic:** fireflies wander a round night pasture; a fox-glow presses them away
  (flee ∝ closeness inside a drawn `INFLUENCE` ring); pen them in the lantern ring. The
  fox *travels* (capped `FOX_SPEED`) — no pointer teleporting, so a lunge is a deliberate
  act, and the lunge detector is pure closing-speed (`prevD − d > LUNGE_CLOSE`).
- **Depth from birth:** the **nick** (lunge into the razor `NICK_BAND` → an aimable
  dead-straight dart; penned mid-dart = +2 + mult ×2…×9), the **panic** one shade deeper
  (wall-blind bolt → stray risk — greed and ruin are the same input), the **Muster**
  (3-nick streak → ~5s double, trigger never doubled), a **secret First Light stage**
  (150 penned), and a **no-plateau liveliness asymptote** (hard-capped, override-proof).
- **Varied structure from birth:** stage-gated flocks — Amble · Scatter · **Moonpool**
  (greed window) · Flicker (temper 1.5 startle radius) · The Split · The Stampede — with a
  pure `placeSpec` guaranteeing in-field + clear-of-lantern spawns *by construction* (the
  Poise lesson: no rejection loop that can give up). Fresh motes carry a `GRACE` fade-in
  (inert), so frame-one can never pen, panic or stray (pinned).
- **Tests:** 42 pure-core tests (physics, dart/panic/push triage, muster trigger-not-
  doubled, dart-can-never-stray, determinism, distinct-seeds → distinct flock sequences,
  placeSpec guarantees, meta). Collection **704/704** green (662 → 704).
- **Wired:** `_games/drove.md` (Herding · Strategy), player changelog (kind `new`),
  `npm run gen:readme`, 512×512 code-rendered icon (fox-glow + dart-line of fireflies +
  lantern ring, eyeballed at card size). Coins: **Prism wisps** fun mode (1 coin,
  additive-render only).
- **Release:** MINOR → **v0.25.0** via `release/0.25.0` → PR → tag → back-merge.

---


_Scheduled EXPAND run (`fairyfox-games-daily`)._

## v0.24.14 — Loft gets "depth inside the mechanic"

- **Maintain:** no open issues; PRs #46/#31 (Dependabot majors) remain deliberately held
  (per status.md — take them as reviewed bumps, not drive-bys).
- **Pick:** status.md → Next named **Loft** the depth-rollout default (the other
  original-seven game). Its no-plateau gravity asymptote shipped in v0.22.1, so this pass
  added the other three items on the one tap verb. Plan: `../../plans/2026-07-21-loft-depth.md`.
- **The Swoop** (hidden tech, taught nowhere): the drawn red danger glow hides a razor
  `SWOOP_BAND` 44px rescue window — a floor-graze catch pays +2, blooms gold, builds a
  streak; a comfortable catch silently breaks it, a whiff leaves it alone. The strike is
  what launches the orb clear, so the rescue *is* the tech (daring by construction).
- **The Tailwind** (the reversal): 3 swoops in a row → ~5s where every point doubles;
  trigger never doubled; gold colour-only (orb rings + toast), reduced-motion honoured.
- **Secret Stratosphere stage** past Zero-G (score 240, gold, reveal toast + badge); the
  start tip trimmed (stage ladder + current names no longer printed).
- **Mechanics:** `applyTap` → rich `TapResult`; TickResult gains `swooped`/`tailLit`;
  3 new badges (8 → 11); `totals.swoops` lossless; game-over line counts swoops.
- **Tests:** +10 (43 → 53); the two shape-pinned tests updated (inert tick result,
  normalizeMeta totals); collection **662/662** green.
- **Logged for players:** `_data/changelog.json` entry (kind `grow`) + `_games/loft.md`
  `updated` → 2026-07-21 + README re-gen.
- **Depth-layer rollout: 8 of 13** (Polarity, Brim, Echo Chamber, Ink Bloom, Orbit
  Slingshot, Ricochet, Skyline, **Loft**; Tether + Reprise + Ward born with it).
  Remaining: Poise, Symmetry, Arc, Sluice.
