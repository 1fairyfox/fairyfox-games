# 2026-07-19

## GROW run (scheduled `fairyfox-games-daily`) — Skyline gets the depth layer (v0.24.3)

- **Maintain:** no open issues; open PRs are only the two held Dependabot majors (#46
  `actions/setup-node` 7, #31 `attest-build-provenance` 4) — deliberately left for a
  deliberate pass, unchanged.
- **Target pick:** depth rollout stood at 6/13; of the remaining (Skyline, Loft, Poise,
  Symmetry, Arc, Sluice), **Skyline is the oldest** and had the sharpest plateau —
  `speedOf` linear, hard-capped at 9.5 from score ≈ 44 (the sweep item's exact shape).
  Plan: `plans/2026-07-19-skyline-depth.md`.
- **Built:** the **Keystone** (razor 1.2px sub-window inside the 3.5px flush window → +2,
  gold bloom, streak; taught nowhere), the **Jet Stream** (3 keystones in a row → next 3
  placed drops pay ×2; trigger never doubled; live slab burns gold while it holds), the
  **no-plateau speed asymptote** (SPAN 7.1 / K 60, cap 10.5; still climbing at score 600,
  pinned), and the **secret Exosphere stage** (score 240, gold, reveal toast + badge).
  3 new badges (8 → 11), `totals.keystones` lossless meta upgrade, run-report keystones,
  start tip trimmed to a curiosity hook (no stage ladder / wind list).
- **Tests:** Skyline 38 → **49**; collection **641/641** green (scoped `games/**`).
  Reworked the two tests that pinned the old linear ramp / exact-flush perfects.
- **Preview:** headless Chrome (server + `--virtual-time-budget`): menu with the trimmed
  tip; a forced deep frame — gold EXOSPHERE chip, gold reveal toast, jet-stream slab
  burning gold over the tower — desktop + 400w mobile, no clipping/overlap. First probe
  shot froze the start panel mid-fade (virtual-time artifact, known); probe driver hides
  the panel and re-shot clean. Probe files deleted before commit. **A live play-feel
  eyeball is still worth doing** (knobs: `KEYSTONE_EPS` 1.2 / `KEYSTONE_TRIGGER` 3 /
  `JET_DROPS` 3, and the asymptote `SPEED_SPAN` 7.1 / `SPEED_K` 60).
- **Logged:** player-facing `_data/changelog.json` entry + `_games/skyline.md` date bump +
  README re-gen; VERSION → **0.24.3** (PATCH); released `dev → main` by default on green.
- **Rollout:** depth-inside-the-mechanic **7 of 13** (Polarity, Brim, Echo Chamber,
  Ink Bloom, Orbit Slingshot, Ricochet, **Skyline**; Tether/Reprise/Ward born with it).
  Next lowest-coverage: **Loft** (the other remaining original-seven game).
