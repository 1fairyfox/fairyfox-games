# Changelog — August 2026

Newest first. Plain-English, per commit. See `../version.md` for how this is kept.

## v0.29.0 — 2026-08-03 — PLANT: Span, the collection's 19th game (a new *build/bridge* verb)

A brand-new game, **Span** (`games/span/`) — and a genuinely new verb: **spanning/construction**.
You are not steering, timing a catch, aiming, metering a fill, swinging, remembering, guarding,
herding, weaving, sorting or resisting — you are **growing a length to fit a gap you can see**.
Your fox stands at a ledge; a gap yawns ahead, then the next ledge. **Hold** one control to grow a
beam (it rises), **release** to drop it flat across the gap; the length at release is its reach.
Fall short and the tip drops into the gap; grow it too long and it overshoots and you walk off the
far end; land the tip *on* the ledge and you cross. A miss can overshoot **or** undershoot
(two-sided), which is what makes it its own thing rather than Arc's charge (ballistic) or Brim's
pour (a metered value with lag). Three misses end the run.

**Ships on the depth layer + varied structure + growth architecture from birth:**

- **Depth — the plumb (discovered, never told).** Landing anywhere crosses, but a loose,
  off-centre landing snaps the multiplier to ×1. Land on the drawn **keystone** (centre stone) and
  the multiplier climbs (×2…×9). Tighter still — dead through the exact centre, and **drawn
  nowhere** — is the **plumb**, a razor sub-window (`PLUMB_ABS`/`PLUMB_FRAC` inside the wider
  `KEYSTONE_*` band, both `max(abs, frac·width)` so they stay fair as ledges shrink): it pays a
  flat `PLUMB_BONUS` and builds a streak. Safe play and greedy play share the one button.
- **Truss.** `TRUSS_STREAK` (3) plumbs in a row → `TRUSS_TICKS` (~5 s) where every point doubles
  (the trigger land itself is not doubled).
- **No plateau, two axes.** `growRateOf` (beam grow speed) is a smooth crossings-asymptote up to
  `GROW_CAP`; `widthFactorOf` shrinks the ledge on a smooth asymptote toward `WFAC_MIN`, floored by
  `WIDTH_HARD_MIN` so it stays landable. Neither ever goes dead-flat.
- **Varied structure.** A run is a seeded sequence of named **crossings** (`FORMATIONS`/
  `pickFormation`/`loadFormation`, `minStage`-gated so climbing the stages **opens the pool**):
  **Steady** (calm on-ramp) · **Reach** (gaps lengthen across the run) · **The Bench** (wide
  generous ledges — the deliberate greed window, safe to hunt centre + chain plumbs) · **Stagger**
  (short/long alternation) · **The Narrows** (narrow ledges, precision) · **The Gauntlet** (long
  gaps + narrow ledges, the crescendo). Notable ones flash a quiet name cue.
- **Stage arc** — Footbridge → Causeway → Trestle → Viaduct → Skyway — with a HUD chip + ambient
  tint, plus a **secret Firmament stage** (110 crossings, revealed only by reaching it).
- **Meta-progression.** `span.meta` (lifetime crossings/points/keystones/plumbs + best stage/mult,
  15 skill-safe badges, run-report) — legacy `span.best` preserved — and a **Lanterns** coin fun
  mode (1 coin, additive render only, score untouched).

Pure `tick(g, holding)`-split core (`span.core.js`, no DOM/canvas/timers) with the frame-one guard
(a fresh span starts at beam 0 and only resolves on the release edge, so tick one can never end the
run). Real `node --test` suite (`span.core.test.js`, **34 tests**). Wired into the mesh: `_games/
span.md` (tags **Bridging** · **Precision**), the landing card + `/tags/` page + masthead count all
generate from it, README table synced via `npm run gen:readme` (**19 games**), a player-facing
`kind:new` entry atop `_data/changelog.json`, and a bespoke 512×512 dark-neon `icon.png`.
Collection **855/855** green (821 + 34). **VERSION → 0.29.0** (MINOR — a new game).

## v0.28.2 — 2026-08-02 — POLISH/CI: Reel gets its own icon + branch-sync back-merge grace

Two follow-up fixes after the Reel PLANT (v0.28.0), both surfaced by a post-deploy live review:

1. **Reel's own icon.** The PLANT shipped without a `games/reel/icon.png`, so the landing card
   fell back to the generic `assets/game-fallback.svg` play-triangle while every other game has a
   bespoke tile. Rendered a 512×512 dark-neon icon (a glowing cyan catch on a taut, slightly bowed
   line with a red tension rim — the give-and-take motif, in the game's palette) via a throwaway
   `render-icon.html` → headless Chrome `--screenshot` (the established icon pipeline), then removed
   the render page (only `icon.png` is committed, matching the other games).

2. **`branch-sync` CI false-alarm.** The guard triggers on `push: main` and checks that `dev`
   contains `main`, but the release flow pushes the main-merge and the `dev` back-merge as two
   *separate* pushes — the back-merge lands a few seconds later — so the guard red-X'd the merge
   commit on **every** release (both v0.27.4 and v0.28.0 showed it). Added a short back-merge grace
   window (re-fetch/re-check up to ~5 min) before failing; it passes immediately once `dev` is
   caught up, so scheduled/dispatch runs are unaffected, a genuinely-skipped back-merge still fails,
   and the daily schedule remains the real backstop. No player-facing/gameplay change; no test change.

## v0.28.1 — 2026-08-02 — fix: SonarCloud org key is junebug12851 (kept after the rename)

**Completing the quality-badge wiring.** The SonarCloud scan was failing with *"Organization key
'1fairyfox' does not exist"* — a SonarCloud organization keeps its key after the GitHub account is
renamed, so the org (and project) stay **`junebug12851_*`** even though the repo now lives under
`1fairyfox` on GitHub. Pointed `sonar-project.properties`, `sonar.yml`, and the two README Sonar
badges (quality gate + tech debt) at **`junebug12851_fairyfox-games`** / org `junebug12851`. With
that, all four token-backed quality services resolve: Codecov (coverage upload), OpenSSF Scorecard
(green on the default branch `dev`), CodeFactor (GitHub app), and SonarCloud. No game logic touched.

## v0.28.0 — 2026-08-02 — PLANT: Reel — a give-and-take (resist/yield) game

A new game, **Reel** — the collection's **18th** and a genuinely new verb: **give-and-take**
(resist vs. yield). There's a catch on the line that *fights back*. You hold **one** control
to reel it in; the catch alternates between **calm** (reel freely) and a **surge** (it
thrashes — reeling now spikes the line **tension**). Read the reacting opponent: pour on the
reel while it's calm, ease off the instant it fights. Fill the progress to **land** it; max
the tension and the line **snaps** (three snaps end the run). Distinct from everything we
have — not steering, timing, aiming, metering, swinging, remembering, guarding, herding,
weaving or sorting, but **modulating a sustained pull against a living, reacting force**
(Brim meters *your own* one-shot pour with a fixed lag; Reel is a continuous back-and-forth
with an adversary whose state you must keep reading).

Ships on **varied structure + the depth layer from day one**:
- **Depth = the haul (discovered, not told).** The tick a surge *breaks* opens a razor
  `HAUL_WIN` window — snap back onto the reel in that instant and the catch's spent lunge
  converts to a burst of progress + a growing multiplier (×2…×9). The safe instinct is to
  keep resting; the greedy line is to re-grab at the break — reel a hair too early (still
  mid-surge) and you over-tension toward a snap. Precise = greedy, on the one input.
- **Frenzy** — a 3-haul streak lights a ~5 s window where every point doubles (the trigger
  never doubled).
- **No plateau** — `surgeTenseScale` is a smooth asymptote of catches landed; thrashing gets
  fiercer forever.
- **Varied structure** — six named **temperaments** (Minnow · Steady · Darter · **Sunfish**
  the greed window · Diver · The Leviathan), `minStage`-gated so climbing the stages opens
  the pool; every catch (and every schedule refill) starts calm — the frame-one guard.
- **Stage arc** (Ripple → Current → Undertow → Riptide → Maelstrom) + a **secret The Deep**
  stage, revealed only by reaching it.
- **Meta** (`reel.meta`: lifetime catches/points/hauls + best stage/mult + 13 badges,
  run-report) — legacy `reel.best` preserved — and a **Glimmer** coin fun mode (1 coin,
  additive bioluminescent render only, score untouched).

Pure `tick(g, holding)`-split core + 32 tests. Collection green. Wired into the landing
(`_games/reel.md` → card + count), the README table (`gen:readme`), and the changelog.

## v0.27.4 — 2026-08-02 — GROW: Tether earns a cross-run unlock (Windfall)

**Tether gets the collection's first _cross-run unlock_** — the varied-structure standard's
rule #3 (the meta layer *driving* variation) made real, and the "grow" target chosen as the
least-recently-touched core (untouched since it was planted at v0.21.0). A new anchor-line,
**Windfall**, is absent from the pool until the player has banked **`UNLOCK_WHIPS` (150) whips
across all their runs**, then starts appearing in every deep run thereafter. It gates on
*whips* — the game's skill act — so the reward is tied to mastering the swing, not merely
surviving; skill-safe (a harder line to read, never a power boost) and **announced on the
game-over card** the run it's earned. Windfall itself is a downhill plunge: anchors drop from
the ceiling to the floor of the sky, ropes shorten and arcs quicken with every catch, so there
is no rhythm to lean on — the mirror of the calm Rise, run fast.

Pure-core changes: a new `buildWindfall`, an `unlock(meta)` predicate on the formation, and two
new pure exports — `unlockedFormations(cfg, meta)` (the earned gated ids) and
`newlyUnlockedFormations(prev, next, cfg)` (what to announce). `pickFormation` takes an optional
`unlocked` set (default none → gated formations never appear, so every prior call is unchanged);
`createGame` seeds `g.unlocked` from `opts.meta`; the shell refreshes it each run and leads the
game-over badge list with a warm-accented "New line: Windfall" notice. **+6 pure-core tests
(32 → 38):** unlock absent-until-earned, monotonic-and-not-free, `pickFormation` gating +
determinism + minStage still holds, the announce-once boundary, Windfall spawns stay
reachable/on-field, and `createGame` reads the meta. Player-facing changelog entry prepended to
`_data/changelog.json`; Tether's card `updated` bumped to today. Collection **789/789** green.
