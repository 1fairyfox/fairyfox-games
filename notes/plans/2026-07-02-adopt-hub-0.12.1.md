# Plan — Adopt fairyfox hub standards 0.9.11 → 0.12.1

Date: 2026-07-02 · Branch: `dev` → release `0.9.0` (MINOR) · Standard:
`hub/standards/adopting-updates.md`. Pre-authorized by the standing
`adopt-standards-by-default` ledger entry (covers all `hub/standards/` +
`hub/templates/`); owner also gave an explicit live go-ahead to "apply fully".

## Decisions

- **Full adopt**, not report-only. Backlog accumulated across three check-only runs
  (0.9.14 → 0.12.1) plus the big 0.12.0 RAP fold (six new/updated standards).
- **Branch protection on `main`: ENABLE** (owner confirmed). Releases become PR-based.
- **Legal pages: GENERATE**, modelled on the sibling `random-ai-prompt` legal set,
  rewritten to be accurate to *this* code (see below).
- **`.subnav` (docs-site 0.11.0): SKIP** — the games site is a single-section landing;
  a secondary submenu adds clutter for no navigation gain. Recorded as a deliberate
  divergence (the standard allows a single-section site to omit it).
- **Signed releases (SLSA provenance): N/A / deferred** — the site is static with no
  build artifact to attest and no `release.yml`. Documented as a reasoned partial.
- **Google Fonts self-hosting: DEFER** (follow-up) — the landing loads Fraunces/Inter/
  JetBrains Mono from Google Fonts (IP exposure). Disclose it honestly in the legal
  pages now; self-hosting is a separate change tracked in `next-steps`.
- **Version: MINOR → 0.9.0** — user-facing legal pages + security hardening + a full
  standards adoption is a milestone.

## Work breakdown

### Line-ending hygiene (the CRLF item)
- Add root `.gitattributes` from `templates/project.gitattributes` (`* text=auto eol=lf`
  + `.bat/.cmd/.ps1` CRLF + binaries). Run `git add --renormalize .`; commit any result.

### New shared files (supply-chain + legal)
- `SECURITY.md` (from template; private reporting via GitHub + junehanabi@gmail.com).
- `.github/dependabot.yml` (npm + github-actions, weekly, target `dev`).
- `.github/workflows/branch-sync.yml` (guard: `dev` must contain `main`).
- `legal/{privacy,terms,cookies}.html` — self-contained, fox-themed, relative links.

### CI hardening (supply-chain-hardening)
- Add top-level `permissions: contents: read` to all 3 workflows; elevate per-job.
- SHA-pin every `uses:` with a `# vX.Y.Z` trailing comment.
- Add `legal` to the netlify.yml `_site` assembly copy list (so pages deploy).

### Docs / README
- README: add the canonical grouped badge block (drop un-wired services).

### Vendor standards → `notes/reference/` (closes the dangling-link gap)
- git-workflow, versioning, cross-project-sync, planning, deployment, agent-tooling,
  dependencies, supply-chain-hardening, legal-docs, badges, process-reports.

### CLAUDE.md
- Agent-tooling rule (PowerShell + file tools, never the bash sandbox; `.gitattributes`).
- "Plan before you execute" in the Default Workflow.
- Back-merge invariant + PR-based release (main now protected).
- "Keep the Legal Docs Accurate" section + a notes-table trigger row.

### Notes / release
- `status.md`, today's session log, changelog atop `notes/version/2026-07.md`,
  `VERSION → 0.9.0`, `next-steps`/`future` follow-ups.
- Process report `notes/fairyfox-reports/2026-07-02-adopting-updates.md`.
- Also commit the two untracked prior check-only reports (07-01, 07-02).

## Open items / verification floor
- Full `node --test` green **before and after** (verification floor; pre-auth adopt).
- `git status` clean; `assets/references/` stays ignored.
- Branch protection enabled with the solo config; release via PR; tag by hand
  (no `release.yml` → CI does not own tagging); back-merge `main → dev`.

## Legal accuracy notes (code-checked)
- No accounts, no sign-up, no server-side data. Static HTML + canvas + ES modules.
- Best scores + preferences stored in the browser's **localStorage** (every game shell).
- **No cookies, no analytics, no tracking.**
- **Google Fonts** loaded on the landing page → visitor IP shared with Google on load
  (disclose; recommend self-hosting as a follow-up).
- Hosting: GitHub Pages (`fairyfox.io/fairyfox-games/`) + Netlify (`games.fairyfox.io`) —
  both may log IPs as processors. All-ages content. MIT-licensed. Contact fairy@fairyfox.io.
